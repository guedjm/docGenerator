h2(id="#{me.id}") !{me.title}
if (me.text != "")
    p !{me.text}

each val in me.urls
    div(class="paragraph") !{val.render(doc)}

each val in me.paths
    div(class="paragraph") !{val.render(doc)}
